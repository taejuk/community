<!DOCTYPE html>
<html>
    <head>
        <meta charset = "utf8">
    </head>
    <body>
        <form action="/modifyPost/<%= community %>/<%= post.board_id %>" method="POST">
            <input type="text" name="title" value="<%= post.title %>" autocomplete="off">
            <br>
            <textarea name="body" autocomplete="off"><%= post.body %></textarea>
            <br>
            <input type="submit" value='수정하기'>
        </form>
    </body>
</html>